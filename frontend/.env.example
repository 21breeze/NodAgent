# Leave empty to use the Vite dev proxy or the Docker Nginx /api proxy.
# For a separately hosted frontend, set the backend origin (no trailing slash).
VITE_API_BASE_URL=

# Optional defaults for a fresh browser; both can be changed in the UI.
VITE_DEFAULT_WORKSPACE_ID=
VITE_DEFAULT_USER_ID=demo-user
